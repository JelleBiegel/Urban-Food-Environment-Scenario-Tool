{
  "name": "Urban Food Environment Scenario Tool",

  "dockerComposeFile": "compose.yaml",
  "service": "app",
  "runServices": [
    "app",
    "gama"
  ],

  "workspaceFolder": "/workspaces/Urban-Food-Environment-Scenario-Tool",

  "forwardPorts": [
    8505
  ],

  "portsAttributes": {
    "8505": {
      "label": "Scenario Tool",
      "onAutoForward": "notify",
      "protocol": "http"
    }
  },

  "postCreateCommand": "python -m pip install -r requirements.txt",

  "postAttachCommand": "pgrep -f 'streamlit run app.py.*8505' >/dev/null || nohup python -m streamlit run app.py --server.address 0.0.0.0 --server.port 8505 > /tmp/streamlit.log 2>&1 &",
  
  "shutdownAction": "stopCompose"
}
